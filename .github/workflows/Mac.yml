name: macOS Remote Desktop
on:
  workflow_dispatch:

jobs:
  macos-remote:
    runs-on: macos-latest
    timeout-minutes: 350

    steps:
      - name: Connect Tailscale
        uses: tailscale/github-action@v3
        with:
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}
          hostname: github-mac-runner

      - name: Setup Virtual Display
        run: |
          brew install jakehilborn/jakehilborn/displayplacer
          # Get screen ID and best mode dynamically
          SCREEN_ID=$(displayplacer list | grep "Persistent screen id" | head -1 | awk '{print $NF}')
          echo "Screen ID: $SCREEN_ID"
          displayplacer list
          # Try modes from 0 upward until one works
          for mode in 0 1 2 3 4 5; do
            displayplacer "id:$SCREEN_ID mode:$mode" && echo "Mode $mode applied!" && break || true
          done

      - name: Prevent Sleep & Wake Display
        run: |
          sudo pmset -a sleep 0 displaysleep 0 disksleep 0 powernap 0
          sudo caffeinate -d &
          open -a Finder
          sleep 3
          osascript -e 'tell application "Finder" to activate'
          sleep 2

      - name: Set Runner Password
        run: |
          # Use passwd with expect-style input
          brew install expect
          /usr/bin/expect << 'EOF'
          spawn sudo passwd runner
          expect "New password:"
          send "rewardoxy123\r"
          expect "Retype new password:"
          send "rewardoxy123\r"
          expect eof
          EOF

      - name: Enable VNC Screen Sharing
        run: |
          tccutil reset ScreenCapture 2>/dev/null || true
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -activate -configure \
            -access -on \
            -allowAccessFor -allUsers \
            -clientopts -setvnclegacy -vnclegacy yes \
            -clientopts -setvncpw -vncpw rewardoxy123 \
            -restart -agent -privs -all
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null || true
          echo "VNC ready!"
          sleep 3

      - name: Print Connection Info
        run: |
          echo "================================"
          echo "  IP  : $(tailscale ip -4)"
          echo "  Port: 5900"
          echo "  User: runner"
          echo "  Pass: rewardoxy123"
          echo "================================"

      - name: Keep Alive
        run: sleep 20000
